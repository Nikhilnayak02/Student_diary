version: "3.2"

services:

  flask:
    build: ./flask
    container_name: flask
    restart: always
    # env_file:
    #   - dev.env
    expose:
      - 5000

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "5000:5000"